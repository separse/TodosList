import { UnsubmittedDirective } from './unsubmitted.directive';

describe('UnsubmittedDirective', () => {
  it('should create an instance', () => {
    const directive = new UnsubmittedDirective();
    expect(directive).toBeTruthy();
  });
});
